<%= form_for([@article, @comment]) do |f| %>
	<% @article.errors.full_messages.each do |message| %>
	<div class="be-red white">
		* <%= message %>
	</div><% end %>
	<div class="field">
	<%= f.text_field :title, placeholder: "Título", class:"form-control"  %>
	</div>

	<div class="field"><%= f.text_area :body, placeholder: "Ingrese el texto" , class: "form-control", style:"height:250px;" %>
	</div>
	<div class="field">
		<%= f.submit "Guardar post", class:"btn be-red white" %>
	</div>
<% end %>
